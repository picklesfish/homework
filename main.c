#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include"queue.h"
extern flag;
extern LQ;
int main()
{
    nemu();
    /*
    int status;
    int x,a=4;
    char ch;
    flag=2;
    LQ=(LQueue*)malloc(sizeof(LQueue));
    InitLQueue(LQ,flag);
    EnLQueue(LQ,&a);
    if(flag == 1)
                {
                    status=GetHeadLQueue(LQ,&ch);
                    if(status == ture)
                        printf("队首元素为%c",ch);
                    else
                        printf("队列为空");
                }
                if(flag == 2)
                {
                    status=GetHeadLQueue(LQ,&x);
                    if(status == ture)
                        printf("队首元素为%d",x);
                    else
                        printf("队列为空");
                }

    */
    return 0;
}




